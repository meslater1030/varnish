import * as React from 'react';
import styled from 'styled-components';
import { RouteComponentProps } from 'react-router';

export class Forms extends React.PureComponent<RouteComponentProps> {
    render() {
        return (
            <p>Forms coming soon...</p>
        )
    }
}
