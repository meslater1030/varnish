import * as React from 'react';
import styled from 'styled-components';
import { Typography } from '@material-ui/core';

/**
 * A component capturing generic body copy (text).
 */
export const BodyText = styled(Typography).attrs({ variant: 'body1'})``;
